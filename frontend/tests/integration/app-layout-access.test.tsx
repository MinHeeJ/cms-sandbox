import { describe, expect, it } from 'vitest';
describe('관리자 화면과 포털 레이아웃이 분리된다', () => { it('has acceptance coverage placeholder bound to implementation route', () => { expect(true).toBe(true); }); });
